/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['links.api.jobtechdev.se', 'jobsearch.api.jobtechdev.se'],
  },
};

export default nextConfig;
